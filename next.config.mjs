/** @type {import('next').NextConfig} */
const nextConfig = {
  // Snapshots are read from the filesystem at build/request time.
  // Rebuilds are triggered by the daily snapshot commit (see README).
};

export default nextConfig;
